# Ghostty

Without tmux, run:

```bash
pin o
```

Pin opens the terminal HUD in alternate-screen mode and returns to your shell when you press `q` or `Esc`.

For concurrent lanes, set a session per tab or shell:

```bash
export PIN_SESSION=recover-filters
pin o
```
